en:
  quotas:
    quota:
      already_exists: A quota already exists for this subject-kind-domain combination
      invalid_for_cluster: invalid for the specified cluster
      invalid_for_domain_type: invalid for the specified domain's type

    override_semantics:
      priority_set_mismatch: does not match the set of valid scopes
      priority_duplicates: contains duplicate values

    semantics_types:
      cumulative: Cumulative
      override: Override
